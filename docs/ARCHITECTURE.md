# البنية المعمارية

## نظرة عامة

معاك تطبيق ويب بسيط من طبقتين، بخطوة بناء واحدة للفرونت إند (esbuild، من غير أي إعداد):

```
┌─────────────────────────┐         ┌──────────────────────────┐
│   المتصفح (Browser)      │  HTTP   │   Express Server          │
│                          │────────▶│   (Node.js)                │
│  dist/vendor.js (React)  │  JSON   │                            │
│  dist/app.js             │◀────────│  routes/*.js               │
│  dist/app.css            │         │  middleware/auth.js        │
│  (تلات ملفات مبنية مسبقًا) │         │  utils/access.js, codes.js │
└─────────────────────────┘         │  scheduler.js (polling)    │
                                     └──────────┬─────────────────┘
                                                │ mysql2/promise
                                                ▼
                                     ┌──────────────────────────┐
                                     │   MySQL (schema.sql)      │
                                     └──────────────────────────┘
```

نفس سيرفر الـ Express (`backend/server.js`) بيعمل حاجتين: بيسيرف ملفات الفرونت إند الاستاتيك (`express.static`، مع ضغط gzip وهيدرات كاش) وبيعرض الـ API تحت `/api/*`. يعني مفيش سيرفر منفصل للفرونت إند.

## خطوة البناء (Build)

مفيش Vite ولا Webpack ولا أي إعداد - خطوة واحدة بملف واحد:
[frontend/build.js](../frontend/build.js) بيستخدم esbuild عشان يطلّع تلات ملفات في
`frontend/dist/`:

| الملف | جاي منين | الحجم (مضغوط) |
|---|---|---|
| `dist/vendor.js` | React + ReactDOM (UMD) من `node_modules` | 47 كيلو |
| `dist/app.js` | كل ملفات `js/` متُرجمة من JSX ومدموجة ومصغّرة | 21 كيلو |
| `dist/app.css` | كل طبقات `css/` مدموجة ومصغّرة | 14 كيلو |

```
npm run build         # من مجلد backend
npm run build:watch   # بيعيد البناء مع كل حفظ
npm run dev           # بناء + سيرفر بـ --watch
```

**ليه اتضافت خطوة بناء أصلاً؟** قبل كده الفرونت إند كان بيحمّل React وBabel Standalone من
CDN، وملفات `.jsx` كانت بتتحمّل زي ما هي (`<script type="text/babel">`) وتتترجم في المتصفح
لحظة الفتح. الميزة كانت صفر إعداد؛ التكلفة كانت إن كل مستخدم بينزّل مترجم JSX حجمه ~3 ميجا،
ويستنى 26 ملف يتحمّلوا واحد ورا التاني، وبعدين يستنى الترجمة تخلص على الخيط الرئيسي - كل ده
**قبل** أول بكسل يترسم.

القياس (Lighthouse، موبايل، شبكة مخنوقة) قبل وبعد:

| | قبل | بعد |
|---|---|---|
| نتيجة الأداء | 57 | **99** |
| أول رسم للمحتوى (FCP) | 6.6 ث | **1.2 ث** |
| أكبر عنصر مرسوم (LCP) | 7.3 ث | **2.0 ث** |
| مؤشّر السرعة (Speed Index) | 11.8 ث | **1.9 ث** |
| إجمالي المنقول | 1052 كيلو | **182 كيلو** |

اللي اتعمل: الترجمة اتنقلت لوقت البناء (مفيش Babel في المتصفح خالص)، وReact والخطوط بقوا
مستضافين عندنا بدل CDN خارجي، والملفات الـ 26 بقت 3، وسيرفر Express بقى بيضغط الردود.

**مخرجات `dist/` متسجّلة في git عمدًا** عشان `npm start` يشتغل على أي نسخة جديدة من الريبو
من غير أي خطوة بناء. فيه فحص في CI بيتأكد إن اللي متسجّل هو ناتج المصدر الحالي - يعني لو حد
عدّل في `js/` أو `css/` ونسي يشغّل `npm run build`، الاختبارات بتفشل.

## هيكل المجلدات

```
backend/
  server.js              نقطة الدخول: بيسجّل الـ routes، بيسيرف الفرونت إند، بيشغّل الـ scheduler
  db.js                  MySQL connection pool (mysql2/promise)
  scheduler.js           بيولّد الجرعات المتوقعة، بيحوّل الفايتة لـ missed، بيبعت تذكير بالمواعيد - كل 5 دقايق
  middleware/auth.js     authRequired: بيتحقق من JWT في الهيدر
  utils/access.js        canAccessPatient: هل المستخدم الحالي له صلاحية على مريض معين
  utils/codes.js         توليد كود مشاركة قصير وتوكن دخول طويل، الاتنين فريدين
  routes/                كل ملف = مجموعة endpoints لمورد واحد (auth, patients, medications, doses, appointments, vitals, notifications)
  sql/schema.sql         مصدر الحقيقة الوحيد لبنية القاعدة (شغّله على قاعدة فاضية)
  sql/migration_*.sql    ترقيات تاريخية لقواعد بيانات قديمة - schema.sql فيها التغييرات دي مدموجة بالفعل

frontend/
  build.js               البناء: بيترجم JSX ويدمج ويصغّر → dist/ (مصدر ترتيب كل ملفات css و js)
  index.html             نقطة الدخول - بيحمّل تلات ملفات من dist/ + شاشة إقلاع مكتوبة في الـ HTML نفسه
  dist/                  ناتج البناء (متسجّل في git) - app.css, app.js, vendor.js
  fonts/                 Cairo + Plus Jakarta Sans (woff2) مستضافين عندنا بدل Google Fonts
  sw.js                  Service Worker: بيكاش قشرة التطبيق، وبيسيب /api/* تعدي من غير أي تدخل
  css/fonts.css           تعريفات @font-face للخطوط اللي في fonts/
  css/tokens.css          متغيرات التصميم كلها (ألوان، خطوط، مسافات، ظلال، حركة) + الوضع الداكن
  css/base.css            إعادة ضبط المتصفح، أساسيات النص، شكل التركيز
  css/animations.css      كل الـ @keyframes + كلاسات الحركة + حارس "تقليل الحركة"
  css/materials.css       الخامات: شرايح الأيقونات، الخلفية المحيطة، الزجاج، الحواف المتدرّجة
  css/components.css      عناصر مشتركة: زراير، حقول، كروت، بانرات، سويتشات
  css/modal.css           النافذة اللي كل شاشات الإضافة والتعديل بتتفتح جواها
  css/layout.css          قشرة المتابع: هيدر، محتوى، شريط التابات، شاشة الإقلاع
  css/auth.css            شاشة الدخول + عرض مميزات التطبيق
  css/screens.css         شاشات المتابع (جرعات، أدوية، مواعيد، قياسات، إشعارات، مرضى)
  css/patient.css         شاشة المريض + شيت "حصل إيه؟"   -- التفاصيل في docs/DESIGN.md
  js/api.js               عميل الـ API - كل نداء لـ /api/* بيعدي من هنا، وبيحط JWT في الهيدر تلقائيًا
  js/icons.jsx            كل أيقونات التطبيق كـ SVG paths - ممنوع الإيموجي في الواجهة
  js/app.jsx               المكوّن الجذري: بيدير تسجيل الدخول، التوجيه بين شاشة المريض وشاشة المتابع، وتفضيلات الجهاز
  js/components/*.jsx      باقي المكونات، كل واحد شاشة أو جزء منها
```

## قرارات تصميم أساسية

**١. المريض بيدخل بلينك، مش بباسورد.** كبير السن مش المفروض يتعامل مع تسجيل دخول أو باسورد. المتابع هو اللي بيضيف المريض (`POST /api/patients`)، والسيرفر بيولّد `access_token` عشوائي طويل بيتحط في لينك (`/access/<token>`). فتح اللينك ده = تسجيل دخول تلقائي (`POST /api/auth/access`). راجع [API.md](API.md#access-token) و[DATABASE.md](DATABASE.md).

**٢. شاشتين منفصلتين تمامًا حسب الدور.** `app.jsx` بيوجّه المريض لـ `PatientHome.jsx` (صفحة واحدة، بدون تابات، خط وأزرار أكبر) والمتابع لـ `AppLayout` (لوحة تحكم كاملة بتابات). القرار ده في الكود نفسه (`if (user.role === 'patient') return <PatientHome .../>`)، مش شرط CSS.

**٣. متابعين متعددين لكل مريض.** جدول `patient_caregiver` many-to-many - أكتر من ابن/بنت يقدروا يتابعوا نفس المريض عن طريق كود مشاركة (`link_code`)، وكل واحد فيهم بياخد نسخته من الإشعارات.

**٤. الـ scheduler بيشتغل بـ polling كل 5 دقايق** (`setInterval` جوه نفس عملية Node، مش cron خارجي أو queue). بيعمل 3 حاجات: يولّد جرعات اليوم وبكرة من `medications.times`، يحوّل أي جرعة `pending` عدّت عليها فترة سماح (30 دقيقة) لـ `missed` ويبعت إشعار، ويبعت تذكير بمواعيد خلال 24 ساعة. ده كافي لحجم المشروع الحالي، لكنه مش دقيق للثانية ولا موزّع (single point لو السيرفر وقع فترة، الجرعات هتتولد أول ما يرجع).

**٥. تفضيلات الواجهة (الوضع الداكن، حجم الخط) متخزنة في `localStorage` على الجهاز نفسه، مش في حساب المستخدم.** القرار مقصود: موبايل المريض وموبايل المتابع أجهزة مختلفة، وكل واحد له احتياج مختلف (شوف `app.jsx` تعليق `readBoolPref`/`writeBoolPref`).

## نقاط ضعف معمارية معروفة

- **التذكيرات شغالة بس والتاب مفتوح في المتصفح** (Web Audio + Notification API من جوه الصفحة نفسها). فيه Service Worker بس دوره كاش القشرة وقابلية التثبيت بس - مفيش Web Push، يعني لو المريض قفل المتصفح مفيش تذكير خالص. أكبر فجوة في المشروع حاليًا.
- **تغطية الاختبارات ضعيفة** - فيه اختبارات لتوقيت الجرعات وللـ API، بس منطق الواجهة نفسه (المكوّنات) غير مغطى بأي اختبار.
- **schema.sql وملفات migration_*.sql مش موثقين كمصدر حقيقة واضح** - schema.sql فيها كل حاجة مدموجة (مناسبة لقاعدة جديدة)، والـ migrations تاريخية بس لترقية قواعد قديمة.
